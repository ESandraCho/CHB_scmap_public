# Pinned environment for the CHB cardiac ion-channel expression analysis.
# Verified on Python 3.13. Install into a fresh venv/conda env:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Note: scanpy is intentionally NOT required — the scripts use anndata directly
# (scanpy's numba pin conflicts with numpy>=2.1). Clustering uses leidenalg+igraph.
anndata==0.12.7
numpy==2.3.3
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.6.1
leidenalg==0.11.0
igraph==1.0.0
statsmodels==0.14.4
matplotlib==3.10.8
h5py==3.15.1
openpyxl==3.1.5
